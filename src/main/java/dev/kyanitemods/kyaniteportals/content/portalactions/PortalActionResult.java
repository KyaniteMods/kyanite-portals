package dev.kyanitemods.kyaniteportals.content.portalactions;

public enum PortalActionResult {
    /**
     * Also use {@code SUCCESS} for partial results (e.g., on the client, for a packet-sending action).
     */
    SUCCESS,
    FAILURE
}
